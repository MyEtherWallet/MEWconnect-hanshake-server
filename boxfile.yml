run.config:
  build_triggers:
    - package.json
  cache_dirs:
    - node_modules
  engine: nodejs
  engine.config:
    dep_manager: npm
    runtime: nodejs-9.3
  extra_packages:
    - bash
  build_triggers:
    - package.json
  extra_steps:
    - npm install && npm run build
data.redis :
  image : nanobox/redis:3.2
web.api:
#  ports:
#    - "tcp:8080:3200"
#  routes:
#    - /socket.io
  start:
    - "bash -c 'npm start'"
